---
// /src/pages/lab/index.astro

import { getCollection } from 'astro:content';
import CollectionLayout from '../../layouts/CollectionLayout.astro';
import Lang from '../../components/Lang.astro';

const lab = (await getCollection('lab')).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const pageTitle = {
    zh: '微研究',
    en: 'Mini Lab',
};

const pageDesc = {
    zh: '存放一些源于生活的奇怪脑洞，和没啥用但很有趣的研究',
    en: 'A sandbox for random surveys and curiosities inspired by daily life.',
};
---

<CollectionLayout title={pageTitle} description={pageDesc} items={lab} slugBase="lab" />
